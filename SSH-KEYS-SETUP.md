# Настройка SSH ключей на Windows

Данное руководство описывает процесс создания SSH ключей на Windows и настройку доступа к удаленному Linux серверу.

## 📋 Содержание

1. [Создание SSH ключа на Windows](#создание-ssh-ключа-на-windows)
2. [Копирование публичного ключа на Linux сервер](#копирование-публичного-ключа-на-linux-сервер)
3. [Проверка подключения](#проверка-подключения)
4. [Дополнительные настройки](#дополнительные-настройки)

## 🔑 Создание SSH ключа на Windows

### Метод 1: Использование OpenSSH (Windows 10/11)

OpenSSH клиент встроен в современные версии Windows (10 версии 1809 и выше, Windows 11).

1. **Откройте PowerShell или Command Prompt (cmd)**

2. **Проверьте наличие OpenSSH:**
   ```powershell
   ssh -V
   ```

3. **Создайте SSH ключ:**
   ```powershell
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```
   
   ⚠️ **Важно:** Всегда используйте ed25519! RSA устарел и считается менее безопасным.
   
   Используйте RSA только если ed25519 абсолютно не поддерживается (очень старые версии Windows/серверы):
   ```powershell
   ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
   ```
   ⚠️ **Не рекомендуется:** RSA медленнее и менее безопасен, чем ed25519.

4. **Следуйте инструкциям:**
   - Нажмите Enter для сохранения в стандартную директорию (`C:\Users\ВашеИмя\.ssh\id_ed25519`)
   - Введите пароль для ключа (рекомендуется, но необязательно) или нажмите Enter дважды для пропуска
   - Пароль повышает безопасность: даже если приватный ключ будет украден, им нельзя будет воспользоваться без пароля

5. **Результат:**
   - Приватный ключ: `C:\Users\ВашеИмя\.ssh\id_ed25519` (хранить в секрете!)
   - Публичный ключ: `C:\Users\ВашеИмя\.ssh\id_ed25519.pub` (можно передавать)

### Метод 2: Использование PuTTY (Windows 7/8 или альтернатива)

1. **Скачайте и установите PuTTY:**
   - Скачайте с официального сайта: https://www.putty.org/
   - Или установите через Chocolatey: `choco install putty`

2. **Запустите PuTTYgen:**
   - Найдите "PuTTYgen" в меню Пуск или запустите `puttygen.exe`

3. **Создайте ключ:**
   - Выберите тип ключа: **Ed25519** (рекомендуется и предпочтительно)
   - ⚠️ **Не используйте RSA** - он устарел, медленнее и менее безопасен
   - Если Ed25519 недоступен, используйте **RSA с минимум 4096 битами** (только в крайнем случае)
   - Нажмите **"Generate"**
   - Двигайте мышью над окном для генерации случайности

4. **Сохраните ключи:**
   - Введите комментарий (например, ваш email)
   - Опционально: введите ключевую фразу (пароль) для дополнительной защиты
   - Нажмите **"Save private key"** и сохраните как `id_ppk.ppk`
   - Скопируйте публичный ключ из текстового поля вверху (начинается с `ssh-ed25519`)

### Метод 3: Использование Git Bash

Если у вас установлен Git for Windows, вы можете использовать Git Bash.

1. **Откройте Git Bash**

2. **Выполните команды создания ключа:**
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

3. **Ключи будут созданы в:** `C:\Users\ВашеИмя\.ssh\`

## 📤 Копирование публичного ключа на Linux сервер

### Метод 1: Использование ssh-copy-id (автоматический)

Если на Windows установлен OpenSSH или Git Bash:

```powershell
# Формат команды:
ssh-copy-id username@server_ip

# Пример:
ssh-copy-id user@192.168.1.100
# или
ssh-copy-id root@example.com

# Если используется нестандартный порт:
ssh-copy-id -p 2222 user@server_ip
```

Вас попросят ввести пароль пользователя на сервере (это последний раз, когда он понадобится).

### Метод 2: Ручное копирование (универсальный метод)

1. **Откройте публичный ключ на Windows:**
   
   В PowerShell:
   ```powershell
   type $env:USERPROFILE\.ssh\id_ed25519.pub
   # или
   Get-Content $env:USERPROFILE\.ssh\id_ed25519.pub
   ```
   
   В Git Bash:
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```
   
   **Скопируйте весь вывод** (обычно одна строка, начинающаяся с `ssh-ed25519`)

2. **Подключитесь к серверу:**
   ```powershell
   ssh username@server_ip
   # Пример: ssh root@192.168.1.100
   ```

3. **На сервере выполните:**
   ```bash
   # Создайте директорию .ssh (если её нет) и установите правильные права
   mkdir -p ~/.ssh
   chmod 700 ~/.ssh
   
   # Добавьте ваш публичный ключ в authorized_keys
   echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
   
   # Установите правильные права на authorized_keys
   chmod 600 ~/.ssh/authorized_keys
   ```

   **Важно:** Замените `ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ` на скопированный ранее ключ (одной строкой).

4. **Проверьте содержимое файла:**
   ```bash
   cat ~/.ssh/authorized_keys
   ```
   Должна отобразиться строка с вашим ключом.

### Метод 3: Использование WinSCP или другого SFTP клиента

1. **Откройте WinSCP** и подключитесь к серверу

2. **Откройте файл:** `~/.ssh/authorized_keys` (или создайте, если его нет)

3. **Скопируйте содержимое вашего публичного ключа** и добавьте его в конец файла `authorized_keys` (каждый ключ на новой строке)

4. **Сохраните файл**

5. **Установите правильные права** через SSH терминал:
   ```bash
   chmod 600 ~/.ssh/authorized_keys
   chmod 700 ~/.ssh
   ```

### Метод 4: Если используется PuTTY ключ

Если вы создали ключ через PuTTYgen (формат .ppk), нужно преобразовать его:

1. **Преобразуйте .ppk в OpenSSH формат:**
   - Откройте PuTTYgen
   - Загрузите ваш .ppk файл: **Load** → выберите `id_ppk.ppk`
   - В меню: **Conversions** → **Export OpenSSH key**
   - Сохраните как `id_openssh`

2. **Извлеките публичный ключ:**
   - В PuTTYgen скопируйте публичный ключ из текстового поля
   - Или экспортируйте: **Conversions** → **Export OpenSSH public key**

3. **Скопируйте публичный ключ на сервер** одним из методов выше

## ✅ Проверка подключения

1. **Откройте новый терминал** (закройте старое подключение, если оно было)

2. **Подключитесь к серверу:**
   ```powershell
   ssh username@server_ip
   # Пример: ssh root@192.168.1.100
   ```

3. **Должно подключиться без запроса пароля** (или с запросом пароля только от ключа, если вы его установили)

4. **Если не работает, проверьте:**
   ```bash
   # На сервере проверьте права:
   ls -la ~/.ssh/
   
   # Должно быть:
   # drwx------  .ssh/
   # -rw-------  authorized_keys
   
   # Проверьте логи SSH:
   sudo tail -f /var/log/auth.log
   # или
   sudo journalctl -u ssh -f
   ```

## 🔧 Дополнительные настройки

### Настройка SSH config файла (опционально)

Создайте файл `C:\Users\ВашеИмя\.ssh\config` для удобства:

```
Host myserver
    HostName 192.168.1.100
    User root
    Port 22
    IdentityFile ~/.ssh/id_ed25519

Host production
    HostName example.com
    User deploy
    Port 2222
    IdentityFile ~/.ssh/id_ed25519_production
```

Теперь можно подключаться просто: `ssh myserver`

### Отключение парольной аутентификации (после настройки ключей)

⚠️ **Важно:** Делайте это только после того, как убедитесь, что доступ по ключу работает!

На сервере отредактируйте `/etc/ssh/sshd_config`:

```bash
sudo nano /etc/ssh/sshd_config
```

Найдите и измените:
```
PasswordAuthentication no
PubkeyAuthentication yes
```

Перезапустите SSH:
```bash
sudo systemctl restart sshd
```

### Если забыли добавить ключ на сервер

Если вы заблокировали себя, используйте:
- Консоль провайдера (AWS, DigitalOcean, Azure и т.д.)
- IPMI/iLO/iDRAC консоль
- Восстановление через recovery режим

## 🐛 Решение проблем

### Ошибка "Permission denied (publickey)"

1. Проверьте, что ключ скопирован правильно в `~/.ssh/authorized_keys`
2. Проверьте права доступа:
   ```bash
   chmod 700 ~/.ssh
   chmod 600 ~/.ssh/authorized_keys
   ```
3. Проверьте владельца файлов:
   ```bash
   chown -R $USER:$USER ~/.ssh
   ```

### Ошибка "Bad permissions"

На Windows убедитесь, что файлы в `~/.ssh/` доступны только вам:
```powershell
icacls $env:USERPROFILE\.ssh\id_ed25519 /inheritance:r
icacls $env:USERPROFILE\.ssh\id_ed25519 /grant:r "$env:USERNAME:(R)"
```

### Ключ не используется автоматически

Укажите ключ явно:
```powershell
ssh -i ~/.ssh/id_ed25519 username@server_ip
```

Или настройте `~/.ssh/config` (см. выше)

## 📚 Полезные ссылки

- [OpenSSH документация](https://www.openssh.com/manual.html)
- [Git for Windows](https://git-scm.com/download/win)
- [PuTTY документация](https://www.chiark.greenend.org.uk/~sgtatham/putty/docs.html)
- [SSH ключи - GitHub Guide](https://docs.github.com/en/authentication/connecting-to-github-with-ssh)

## 🔐 Безопасность

- **Никогда не делитесь приватным ключом** (`id_ed25519`, `.ppk`)
- Используйте сильный пароль для ключа (ключевая фраза)
- Регулярно ротируйте ключи
- Используйте разные ключи для разных серверов
- Отключайте парольную аутентификацию после настройки ключей
- Используйте ed25519 вместо RSA (быстрее и безопаснее)

